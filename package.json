{
  "name": "skillinaboxdashboard",
  "version": "1.0.0",
  "private": true,
  "description": "SkillinaBox dashboard static app",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html dist/index.html",
    "start": "python3 -m http.server 4173"
  }
}
